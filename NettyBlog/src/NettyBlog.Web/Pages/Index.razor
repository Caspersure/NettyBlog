@page "/"
@using NettyBlog.Caller.Post

@inject  PostCaller postCaller
<h1>Hello world!</h1>
@code
{
    protected override async Task OnInitializedAsync()
    {
  //    var res =  await postCaller.GetListAsync();
       await base.OnInitializedAsync();
    }
}
